Cmock does not support mocking standard C library.
To bypass that limtation interface to functions (that could be mocked) is introduced.
Now You can mock interface instead of standard library function.
